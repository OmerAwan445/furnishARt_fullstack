import VerifyEmail from "@/components/VerifyEmailPage/VerifyEmail";
import React, { Suspense } from "react";

const VerifyEmailPage = () => {
  return (
    <Suspense>
      <VerifyEmail />
    </Suspense>
  );
};

export default VerifyEmailPage;
